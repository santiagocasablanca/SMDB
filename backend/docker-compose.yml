version: '3'
services:
  svdb:
    image: postgres:latest
    container_name: svdb
    ports:
      - '6505:5432'
    volumes:
      - progresDB:/data/postgres
    env_file:
      - ./.env
volumes:
  progresDB:
